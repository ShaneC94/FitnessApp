<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allows sharing files created in cacheDir -->
    <cache-path
        name="camera_cache"
        path="." />

    <!-- OPTIONAL: allows externalCacheDir too -->
    <external-cache-path
        name="camera_external_cache"
        path="." />
</paths>
