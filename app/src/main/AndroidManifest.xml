<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fitnessapp">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessTracker">

        <!-- ChatGPT OpenAI API Key -->
        <activity android:name=".ui.chat.ChatActivity" />


        <!-- Google Maps API Key (securely injected via Gradle placeholders) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- Launch Activity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication -->
        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />

        <!-- Map Screen -->
        <activity
            android:name=".ui.map.MapActivity"
            android:exported="false" />

        <!-- Recipes -->
        <activity
            android:name=".ui.recipes.RecipesActivity"
            android:exported="false" />

        <activity
            android:name=".ui.recipes.AddRecipesActivity"
            android:exported="false" />

        <activity android:name=".ui.recipes.RecipeDetailActivity" />

        <!-- Workouts -->
        <activity
            android:name=".ui.workouts.WorkoutActivity"
            android:exported="false" />

        <activity
            android:name=".ui.workouts.AddWorkoutActivity"
            android:exported="false" />

        <activity
            android:name=".ui.workouts.WorkoutDetailActivity"
            android:exported="false" />

        <!-- Popular Exercises -->
        <activity
            android:name="com.fitnessapp.ui.popularExercises.PopularExercisesActivity"
            android:exported="false" />

    </application>
</manifest>
